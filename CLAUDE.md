# Agent Skill Kit

Use the canonical modular skills under `skills/` and begin with `user-task-orchestrator` plus the smallest relevant subset. Preserve public/private boundaries and follow `AGENTS.md`.

For Claude Code setup, consult [`docs/CROSS_AGENT_COMPATIBILITY.md`](docs/CROSS_AGENT_COMPATIBILITY.md) and copy only the selected skill directories into the current host-supported project or user skill location. Do not treat this file as a replacement for the canonical `SKILL.md` modules.
